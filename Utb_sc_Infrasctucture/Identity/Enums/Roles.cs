using System;

namespace Utb_sc_Infrastructure.Identity.Enums
{
    /// <summary>
    /// Enumeration for Roles in database (they must match)
    /// Enum is not required but may be useful
    /// </summary>
    public enum Roles
    {
        Admin,
        Moderator,
        User
    }
}